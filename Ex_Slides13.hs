-- 1. Crie uma função que solicita do usuário o caminho de um arquivo e retorna o tamanho dele em
-- caracteres.
tamarquivo :: 

-- 2. Crie uma função que solicita do usuário o caminho de um arquivo e retorna o tamanho dele em
-- linhas.
-- 3. Crie uma função que solicita do usuário o caminho de um arquivo e permite que ele escreva mais
-- linhas ao final do arquivo. Para finalizar, o usuário deve digitar “EOF”.
-- 4. Crie uma função que solicita que o usuário digite “sum” para realizar uma soma ou “sub” para
-- realizar uma subtração. Em seguida, ele solicita que o usuário digite dois operandos e realiza a
-- operação selecionada, imprimindo o resultado na tela no formato “a op b = s”, onde op pode ser ‘+’
-- ou ‘-’, a e b são os operandos e r é o resultado. O usuário pode repetir quantas vezes quiser até
-- digitar “fim” no lugar da operação. O programa deve escrever em um arquivo todas as operações
-- realizadas no formato “a op b = s”, cada uma em uma linha.